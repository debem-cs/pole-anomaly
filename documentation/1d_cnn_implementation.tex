\section{Reference PyTorch Implementation: 1D-CNN}

To validate the proposed architectures, a functional baseline implementation of the 1D-CNN was developed using PyTorch. The architecture has been specifically designed for \textbf{Multi-Class Classification}, capable of distinguishing between various anomaly templates (Bell, Square, M, etc.). The codebase is organized into modular scripts within the \texttt{1D-CNN} directory, with full CUDA GPU acceleration support.

\subsection{Data Processing (\texttt{dataset\_loader.py})}
The \texttt{dataset\_loader.py} module bridges the synthetic \texttt{.csv} generator and the neural network. It performs the following steps:
\begin{enumerate}
    \item \textbf{Temporal Split:} The continuous signal is split chronologically \textit{before} windowing: the first 80\% of time steps are reserved for training and the remaining 20\% for testing. This prevents \textbf{data leakage}, which would occur with a random split because overlapping windows (stride 32 over a 512-point window) share up to 94\% of their data points.
    \item \textbf{Sliding Window Extraction:} Each split is independently sliced into overlapping windows (configurable size, default 512, stride 32).
    \item \textbf{Threshold-Based Labeling:} A window is only labeled as a specific anomaly class if the anomaly occupies at least \textbf{10\%} of the window length. This prevents the CNN from learning that windows containing 99\% pure noise should be classified as anomalies. When the threshold is met, the most frequent anomaly class ID within the window is assigned via majority voting (\texttt{np.unique} with \texttt{return\_counts}).
    \item \textbf{Per-Window Centering (Instance Normalization):} To make the network robust against the slowly wandering background baseline generated by the Ornstein-Uhlenbeck process, each sliding window is independently centered by subtracting its own local mean $\mu_{\mathrm{window}}$. However, a global standard deviation $\sigma_{\mathrm{train}}$ is computed across all centered training windows to preserve the relative amplitudes of the spikes. This prevents the model from "squashing" small anomalies into looking like normal noise. The test set is scaled using this same global statistic. Critically, the global standard deviation is persisted to \texttt{normalization\_stats.json} so that the inference pipeline applies \textit{identical} amplitude scaling:
    \begin{equation}
        x_{\mathrm{scaled}} = \frac{x - \mu_{\mathrm{window}}}{\sigma_{\mathrm{train}}}
    \end{equation}
    \item \textbf{Tensor Reshaping:} The output is reshaped into the strict \texttt{(Batch\_Size, 1, Window\_Size)} format required by PyTorch's \texttt{Conv1d} layers.
\end{enumerate}

\subsection{Architecture Definition (\texttt{model.py})}
The \texttt{Anomaly1DCNN} class inherits from \texttt{torch.nn.Module} and implements a 3-block convolutional architecture with a progressively narrowing kernel strategy to balance global structure capture with fine-grained feature extraction:
\begin{itemize}
    \item \textbf{Block 1:} \texttt{Conv1d(in=1, out=16, kernel=7, padding=3)} $\rightarrow$ \texttt{BatchNorm1d} $\rightarrow$ \texttt{ReLU} $\rightarrow$ \texttt{MaxPool1d(2)}.
    \item \textbf{Block 2:} \texttt{Conv1d(in=16, out=32, kernel=7, padding=3)} $\rightarrow$ \texttt{BatchNorm1d} $\rightarrow$ \texttt{ReLU} $\rightarrow$ \texttt{MaxPool1d(2)}.
    \item \textbf{Block 3:} \texttt{Conv1d(in=32, out=64, kernel=5, padding=2)} $\rightarrow$ \texttt{BatchNorm1d} $\rightarrow$ \texttt{ReLU} $\rightarrow$ \texttt{MaxPool1d(2)}.
\end{itemize}
The early layers use larger kernels (7, 7) to capture broad temporal structure such as the overall envelope of anomaly shapes, while the final layer uses a smaller kernel (5) to refine local features. The network utilizes a Regional Pooling layer (\texttt{AdaptiveAvgPool1d(4)}) instead of Global Average Pooling. This preserves 4 distinct temporal bins, allowing the network to recognize the temporal topography of shapes (e.g., distinguishing the two separate peaks of an \texttt{M} shape from a solid \texttt{m\_sq} block).

This flattened sequence vector of size 256 ($64 \times 4$) is then passed through a classifier head composed of \texttt{Flatten()} $\rightarrow$ \texttt{Dropout(0.4)} $\rightarrow$ \texttt{Linear(256, 32)} $\rightarrow$ \texttt{ReLU} $\rightarrow$ \texttt{Dropout(0.2)} $\rightarrow$ \texttt{Linear(32, num\_classes)}. The dropout layers serve as regularization to prevent overfitting. The total model footprint totals approximately 22,727 trainable parameters.

\subsection{Training Loop (\texttt{train.py})}
The training script establishes the complete PyTorch training ecosystem with GPU acceleration and comprehensive logging:
\begin{itemize}
    \item \textbf{Data Split:} Since the dataset loader already performs the temporal train/test split, the training script further divides the training set 80/20 into train/validation subsets using a random permutation for early stopping monitoring.
    \item \textbf{Class Imbalance Handling:} Uses \texttt{np.bincount} to dynamically calculate the distribution of each class. It computes inverse frequency weights as $w_c = \frac{N_{\mathrm{total}}}{C \cdot N_c}$ and passes them to the \texttt{CrossEntropyLoss} criterion, forcing the optimizer to treat rare anomaly shapes with the same importance as the dominant normal background.
    \item \textbf{Optimization:} Employs the Adam optimizer with an initial learning rate of $10^{-3}$.
    \item \textbf{Learning Rate Scheduling:} A \texttt{ReduceLROnPlateau} scheduler (patience 2 epochs, factor 0.5) monitors the validation loss and halves the learning rate when progress stalls. This reduces the oscillatory behavior observed in the validation loss during later training stages.
    \item \textbf{Early Stopping:} A patience-based mechanism (5 epochs) monitors the validation loss. The best model weights are saved to \texttt{best\_1d\_cnn\_pytorch.pth} only when the validation loss improves.
    \item \textbf{Evaluation:} After training, the best checkpoint is reloaded and evaluated on the held-out test set. A per-class accuracy breakdown is computed and logged.
    \item \textbf{Logging:} A \texttt{.txt} log file is generated in the \texttt{logs/} directory, recording all hyperparameters, environment details (PyTorch version, GPU model, CUDA version), class distributions, per-epoch loss curves with current learning rate, training duration, and per-class test accuracy.
\end{itemize}

\subsection{Architecture Search}
The final architecture was selected through an empirical search over several candidate configurations. All variants were trained on the same 10M-point synthetic dataset (249,985 training windows) and evaluated on both per-window test accuracy and event-level inference over a 100,000-point segment containing 77 anomaly events. Key findings:

\begin{itemize}
    \item \textbf{Depth vs.\ Width:} Reducing the number of convolutional blocks below 3 degrades performance unless channel width is increased to compensate. A 2-block model with narrow channels (16$\rightarrow$32) lacked sufficient representational capacity, while a 2-block model with wider channels (32$\rightarrow$64) recovered most of the accuracy.
    \item \textbf{Diminishing returns of depth:} Blocks 4 and 5 from the original architecture contributed negligible accuracy gains while inflating the parameter count by $12\times$.
    \item \textbf{Training speed:} Tightening the early stopping patience from 10 to 5 epochs and the LR scheduler patience from 4 to 2 epochs, combined with doubling the batch size to 128, reduced training time significantly without affecting convergence quality.
\end{itemize}

\begin{table}[H]
\centering
\caption{Comparison of 1D-CNN architecture variants. Event-level metrics are computed over 77 anomaly events in a 100K-point inference segment. All models use \texttt{AdaptiveAvgPool1d(4)} regional pooling.}
\label{tab:architecture_comparison}
\small
\begin{tabular}{|l|c|c|c|c|c|}
\hline
\textbf{Variant} & \textbf{Channels} & \textbf{Params} & \textbf{Train Time} & \textbf{Event Acc.} & \textbf{Mean Conf.} \\
\hline
2-block narrow  & 16-32              & 3,719    & 8 min 12 s  & 88.3\%        & 66.8\% \\
\hline
2-block wide    & 32-64              & 19,207   & 5 min 49 s  & 98.7\%        & 79.7\% \\
\hline
\textbf{3-block} & \textbf{16-32-64}  & \textbf{22,727} & \textbf{5 min 45 s} & \textbf{100\%} & \textbf{90.2\%} \\
\hline
4-block         & 16-32-64-64        & 35,207   & 7 min 18 s  & 100\%         & 90.2\% \\
\hline
5-block        & 32-64-128-128-256  & 270,725  & 7 min 58 s  & 100\%         & 91.9\% \\
\hline
\end{tabular}
\end{table}

The 3-block architecture was selected as the final model, achieving perfect event-level classification with $12\times$ fewer parameters than the 5-block architecture and the fastest training time. The 2-block wide variant is a viable lightweight alternative at the cost of one misclassification on a very short 74-point event.

\subsection{Inference and Evaluation (\texttt{visualize\_inference.py})}
To interpret the model's behavior on streaming data, a visualization and evaluation script runs the trained model over a continuous segment of the dataset using a fine-grained sliding window (stride of 10). Inference is performed on GPU when available. Two key design decisions distinguish this inference pipeline from naive approaches:

\begin{enumerate}
    \item \textbf{Consistent Normalization:} The inference script loads the global $\sigma_{\mathrm{train}}$ value saved during training from \texttt{normalization\_stats.json}. It applies the exact same per-window centering strategy by dynamically calculating $\mu_{\mathrm{window}}$ during the sliding pass. This ensures the model is perfectly invariant to vertical baseline drift while seeing the same scale distribution it was trained on.
    \item \textbf{Overlapping Window Accumulation:} Instead of assigning each window's prediction to a single center point, the softmax probability vector is \textbf{accumulated} across all points covered by the window. With stride 10 and window size 512, each point is covered by up to 51 overlapping windows. The final per-point class probability is the average across all contributing windows:
    \begin{equation}
        P_{\mathrm{final}}(c \mid t) = \frac{1}{|\mathcal{W}_t|} \sum_{w \in \mathcal{W}_t} P_w(c)
    \end{equation}
    where $\mathcal{W}_t$ is the set of all windows covering time step $t$. This ensemble-like averaging significantly reduces prediction noise.
\end{enumerate}

The script produces the following outputs:
\begin{enumerate}
    \item \textbf{Interactive Visualization:} An interactive dual-axis Plotly graph rendering the simulated gamma noise on the primary axis and distinct, colored probability curves for each anomaly class on the secondary axis. A rolling average (window of 30) is applied to smooth the accumulated probabilities.
    \item \textbf{Event-Level Evaluation:} Rather than evaluating per-point predictions---which are misleading due to sliding window boundary spillover inflating false positives---the evaluation groups contiguous anomaly points into discrete \textbf{events}. For each event, the model's accumulated probability vectors are averaged and the \texttt{argmax} class is compared against the ground truth label. The log reports a per-event results table, an event-level confusion matrix, and derives per-class Precision, Recall, and F1-Score at the event granularity, along with the overall detection rate and exact classification accuracy.
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{media/cnn_inference_visualization.png}
    \caption{Multi-Class 1D-CNN Confidence Probability mapped against Simulated Gamma Noise}
    \label{fig:cnn_visualization}
\end{figure}

\subsection{Training Results}
The selected 3-block architecture (22,727 parameters) was trained on a 10M-point synthetic dataset comprising 249,985 sliding windows (512 points, stride 32) split temporally into 199,988 training, 49,997 validation, and 62,485 test windows. Training was performed on an NVIDIA GeForce RTX 5060 Laptop GPU using PyTorch with CUDA 12.8 acceleration. Key training outcomes:

\begin{itemize}
    \item \textbf{Convergence:} The model converged in 82 epochs (9 minutes 5 seconds), with the learning rate decaying from $10^{-3}$ to $8 \times 10^{-6}$ across 7 reduction steps before early stopping triggered.
    \item \textbf{Final losses:} Best validation loss = 0.2494, test loss = 0.3028.
    \item \textbf{Test accuracy:} 93.4\% per-window accuracy across 62,485 windows (note: event-level accuracy is significantly higher due to overlapping window accumulation during inference).
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{media/training_curves.png}
    \caption{Training and validation loss curves with learning rate schedule for the 3-block 1D-CNN (22,727 parameters). The validation loss consistently stays below the training loss due to class-weighted cross-entropy inflating the training loss for minority anomaly classes.}
    \label{fig:training_curves}
\end{figure}

\subsection{Inference Results}
The trained model was evaluated on a 1,000,000-point segment of the dataset containing 777 anomaly events across 6 anomaly classes. Inference was performed using a sliding window (stride 10, window 512) with overlapping probability accumulation (Section~\ref{fig:cnn_visualization}).

\begin{table}[H]
\centering
\caption{Event-level inference results over 777 anomaly events in a 1M-point segment. Detection rate indicates whether the model identified the region as any anomaly type; exact classification requires the correct class.}
\label{tab:inference_results}
\small
\begin{tabular}{|l|c|}
\hline
\textbf{Metric} & \textbf{Value} \\
\hline
Total events & 777 \\
\hline
Detection rate (any anomaly) & 100.0\% (777/777) \\
\hline
Exact classification accuracy & 98.3\% (764/777) \\
\hline
Mean confidence & 88.8\% \\
\hline
Inference throughput & 1,421 windows/sec \\
\hline
Inference time (1M points) & 70 s \\
\hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Per-class Precision, Recall, and F1-Score at event-level granularity.}
\label{tab:per_class_metrics}
\small
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Class} & \textbf{Support} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
\hline
bell & 128 & 0.992 & 0.992 & 0.992 \\
\hline
bell\_sq & 136 & 0.963 & 0.956 & 0.959 \\
\hline
fast\_ascend & 129 & 1.000 & 0.992 & 0.996 \\
\hline
fast\_descend & 116 & 0.983 & 0.991 & 0.987 \\
\hline
M & 126 & 0.962 & 0.992 & 0.977 \\
\hline
square & 142 & 1.000 & 0.979 & 0.989 \\
\hline
\textbf{Macro Average} & & \textbf{0.983} & \textbf{0.984} & \textbf{0.984} \\
\hline
\end{tabular}
\end{table}

The 13 misclassifications are concentrated in the \texttt{bell\_sq} class (6 errors), which is the most morphologically ambiguous shape---its flat-topped profile can be confused with \texttt{square} at certain deformation levels, and its rounded shoulders can resemble \texttt{M} when the shape variance is high.

\subsection{Robustness Analysis}
To assess the model's sensitivity to degraded signal conditions, a robustness test was conducted by independently sweeping two axes while holding the other at its training baseline:
\begin{enumerate}
    \item \textbf{Background noise intensity:} The standard deviation of the high-frequency sensor noise was scaled from $1\times$ to $3\times$ the baseline value ($\sigma = 2.70$ gamma dose units).
    \item \textbf{Anomaly shape deformation:} The variance parameter of the anomaly generator was scaled from $1\times$ to $8\times$ the baseline value (0.04), controlling how much each anomaly instance deviates from its canonical template shape.
\end{enumerate}

Each configuration generated a test dataset with 50 anomaly events using a fixed random seed to ensure identical anomaly positions and template selections across all runs. The model was evaluated at event-level granularity on each dataset.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{media/robustness_test.png}
    \caption{Model robustness under increasing noise (left) and shape deformation (right). Event accuracy (solid) and mean confidence (dashed) are shown. The model is substantially more sensitive to shape deformation than to background noise.}
    \label{fig:robustness}
\end{figure}
